# Suggested résumé entries

## Hospital Data Lab | Python, SQLite, pandas, Streamlit | September 2026

- Built a reproducible Python pipeline generating 10,000 fictional hospital visits across five departments, with SQLite analytics and an interactive Streamlit dashboard for wait-time and visit-volume analysis.
- Implemented 10 validation rules that detected all 100 deliberately corrupted rows in a controlled dataset, quarantining invalid records while retaining 9,900 valid visits; verified the pipeline and dashboard with 42 automated tests.

These are measured project results. Do not describe them as hospital deployments, clinical outcomes, hackathon results, or performance improvements over an unmeasured baseline.

## Java File System Simulator | Java, GitHub Actions

Use this in place of the C Game Store project after uploading the Java repository. Add the dates that reflect your work and the real repository URL once created.

- Built an in-memory Java file-system simulator supporting relative-path resolution, recursive folder sizing, and independent subtree copies through an n-ary tree model.
- Protected tree integrity with cycle detection, duplicate-name validation, and move checks that preserve source folders on failure; passed 47 regression checks covering paths, copies, moves, and invalid inputs.

The Java package was delivered separately as java-file-system-simulator.zip. GitHub Actions is configured; do not say its hosted workflow passed until GitHub has run it.
